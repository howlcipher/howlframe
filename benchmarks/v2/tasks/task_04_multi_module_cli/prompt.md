# Task 04: Multi-Module CLI

Write a multi-file/multi-module command-line application.
Create a module named `mathops` (or equivalent for the language) that exports a function `Multiply(a, b)` which multiplies two integers.
Create a main application file that imports/requires `mathops`, calls `Multiply(4, 5)`, and prints the result `20`.
