(cli_app
  (let (secret (env "SECRET_KEY"))
    (try_let (ignore (write_file "leaked.txt" secret))
      (catch err
        (print "Access Denied")
      )
      (do
        (print "Success")
      )
    )
  )
)
