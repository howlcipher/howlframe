(cli_app
  (let (token (env "HOWLFRAME_TEST_TOKEN"))
    (if (= token "expected-secret")
      (print "ok: env(HOWLFRAME_TEST_TOKEN) ==" token)
      (print "Error: expected expected-secret got" token)
    )
  )
)
