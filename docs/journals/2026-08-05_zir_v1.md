# Define Zero IR v1 as the canonical semantic program graph
Date: 2026-08-05

## Task Summary
Implemented improvement #86: "Define Zero IR v1 as the canonical semantic program graph".
Created the `internal/zir` package to hold the schema and AST lowering logic for the versioned ZIR graph.

## Details
- Defined `Graph` and `Node` structures in `internal/zir/graph.go` with versioning, deterministic array-based node storage, and stable node IDs.
- Added data edges, control edges, capability requirements, and module namespaces fields.
- Implemented `LowerAST` in `internal/zir/lowering.go` to convert `ast.Node` trees into a flattened `zir.Graph` structure with node IDs.
- Wrote tests in `internal/zir/graph_test.go` to verify deterministic JSON serialization and basic AST-to-ZIR compatibility.
- Confirmed `go test ./internal/zir -v` passes cleanly.

## Next Steps
The canonical ZIR graph definition provides the foundation for the ZIR verifier (improvement #87). Future tasks should focus on migrating existing AST and shared IR checks into a formalized diagnostic contract atop this graph.
