# Define HFIR v1 as the canonical semantic program graph
Date: 2026-08-05

## Task Summary
Implemented improvement #86: "Define HFIR v1 as the canonical semantic program graph".
Created the `internal/hfir` package to hold the schema and AST lowering logic for the versioned HFIR graph.

## Details
- Defined `Graph` and `Node` structures in `internal/hfir/graph.go` with versioning, deterministic array-based node storage, and stable node IDs.
- Added data edges, control edges, capability requirements, and module namespaces fields.
- Implemented `LowerAST` in `internal/hfir/lowering.go` to convert `ast.Node` trees into a flattened `hfir.Graph` structure with node IDs.
- Wrote tests in `internal/hfir/graph_test.go` to verify deterministic JSON serialization and basic AST-to-HFIR compatibility.
- Confirmed `go test ./internal/hfir -v` passes cleanly.

## Next Steps
The canonical HFIR graph definition provides the foundation for the HFIR verifier (improvement #87). Future tasks should focus on migrating existing AST and shared IR checks into a formalized diagnostic contract atop this graph.
